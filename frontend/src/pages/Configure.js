import React from 'react';

const Configure = () => {
    return (
        <div>
            <h2>Configure Page</h2>
            <p>Only accessible by users with elevated roles.</p>
        </div>
    );
};

export default Configure;